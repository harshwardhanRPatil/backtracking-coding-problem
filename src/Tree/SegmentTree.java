package Tree;


/*
Segment Tree is used when we need to handle frequent range queries
(like sum, min, max) along with updates on an array.
It allows both queries and updates in O(log n) time.
Commonly used when brute-force range queries (O(n)) are too slow.
It aslo a full binary tree
*/


public class SegmentTree {
    public static void main(String args[]){

    }
}
